name: CI/CD

on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '22'
  DOTNET_VERSION: '10.0.x'  # Updated to match project target framework

jobs:
  # Matriz de Qualidade (Launch)
  # - Bloqueantes: backend, frontend, e2e (quebram PR/main)
  # - Informativos: quality-informational (não bloqueia merge/deploy)

  # =============================================
  # Backend: Build + Test
  # =============================================
  backend:
    name: Backend (API)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: schoolmanagement_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GH_PAT }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore
        working-directory: school-management-api

      - name: Build
        run: dotnet build --no-restore --configuration Release
        working-directory: school-management-api

      - name: Test
        run: dotnet test --no-build --configuration Release --logger trx --results-directory ./test-results
        working-directory: school-management-api
        env:
          ConnectionStrings__DefaultConnection: "Host=localhost;Port=5432;Database=schoolmanagement_test;Username=test;Password=test"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: school-management-api/test-results/*.trx

  # =============================================
  # Frontend: Lint + Build + Unit Test
  # =============================================
  frontend:
    name: Frontend (Build & Test)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: school-management-ui/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: school-management-ui

      - name: Lint
        run: npm run lint
        working-directory: school-management-ui

      - name: Build
        run: npm run build -- --configuration production
        working-directory: school-management-ui

      # Use the karma config's default ChromeHeadlessNoSandbox (with --no-sandbox flag)
      - name: Unit Test (Karma/Jasmine)
        run: npm test -- --no-progress --code-coverage
        working-directory: school-management-ui

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: school-management-ui/coverage/

  # =============================================
  # Informational checks (non-blocking)
  # =============================================
  quality-informational:
    name: Quality (Informational)
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: school-management-ui/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: school-management-ui

      - name: Dependency audit (informational)
        run: npm audit --audit-level=high || true
        working-directory: school-management-ui

      - name: Dependency outdated report (informational)
        run: npm outdated || true
        working-directory: school-management-ui

  # =============================================
  # E2E Tests (Cypress)
  # =============================================
  e2e:
    name: E2E Tests (Cypress)
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: schoolmanagement_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: school-management-ui/package-lock.json

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install UI dependencies
        run: npm ci
        working-directory: school-management-ui

      - name: Restore API dependencies
        run: dotnet restore
        working-directory: school-management-api

      - name: Build API
        run: dotnet build --no-restore --configuration Release
        working-directory: school-management-api

      - name: Start API (background)
        run: |
          nohup dotnet run --no-build --configuration Release \
            --project src/SchoolManagement \
            --urls "http://127.0.0.1:5067" \
            > /tmp/api.log 2>&1 &
          echo "PID=$!"
        working-directory: school-management-api
        env:
          ASPNETCORE_ENVIRONMENT: Development
          ConnectionStrings__DefaultConnection: "Host=localhost;Port=5432;Database=schoolmanagement_test;Username=test;Password=test"

      - name: Wait for API
        run: |
          echo "⏳ Waiting for API on port 5067..."
          for i in $(seq 1 90); do
            if curl -sf http://127.0.0.1:5067/__debug/https-test > /dev/null 2>&1; then
              echo "✅ API is ready!"
              exit 0
            fi
            echo "⏳ Attempt $i/90..."
            sleep 2
          done
          echo "❌ API failed to start"
          cat /tmp/api.log
          exit 1

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          working-directory: school-management-ui
          start: npm run start:e2e
          wait-on: 'http://127.0.0.1:4200,http://127.0.0.1:5067/__debug/https-test'
          wait-on-timeout: 120
          record: false
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:4200

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: school-management-ui/cypress/screenshots

      - name: Upload Cypress videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: school-management-ui/cypress/videos

  # =============================================
  # Deploy (só na main)
  # =============================================
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: [backend, frontend, e2e]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GH_PAT }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: school-management-ui/package-lock.json

      # Build API
      - name: Publish API
        run: |
          dotnet publish school-management-api/src/SchoolManagement/SchoolManagement.csproj \
            -c Release -o ./publish/api
        working-directory: .

      # Build UI
      - name: Build UI
        run: |
          npm ci
          npm run build -- --configuration production
        working-directory: school-management-ui

      # Build Docker images
      - name: Build Docker images
        run: |
          docker build -t schoolmanagement-api:latest -f school-management-api/Dockerfile school-management-api/
          docker build -t schoolmanagement-ui:latest -f school-management-ui/Dockerfile school-management-ui/

      - name: Deployment summary
        run: |
          echo "✅ Build completed successfully"
          echo "📦 API: ./publish/api"
          echo "🌐 UI: school-management-ui/dist/"
          echo "🐳 Docker images: schoolmanagement-api:latest, schoolmanagement-ui:latest"
